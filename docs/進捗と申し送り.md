# 時計価格ナビ — 進捗と申し送り

このファイルは**セッションが切れても作業を再開できるようにする**ための記録です。
作業のたびに更新します。新しいセッションを始めたら、まずこれを読んでください。

最終更新: 2026-08-16（Instagram投稿が稼働。トークンは自動更新）

---

## 1. これは何か

腕時計の価格比較サイト。楽天市場・Yahoo!ショッピングの出品価格を毎日自動収集し、
型番ごとに最安値を並べる。全自動で運用し、アフィリエイトと買取リードで収益を得る。

- 公開URL: https://watch-price-navi.github.io/watch-price-navi/
- リポジトリ: https://github.com/watch-price-navi/watch-price-navi （**Public**。秘密情報を絶対に置かない）
- 掲載: 39ブランド / 29,269モデル（人手968 + 自動28,301）
- 構成: Next.js 15 App Router / `output: 'export'` の静的書き出し / GitHub Pages

---

## 2. 自動運用（触らなくても毎日動く）

GitHub Actions `.github/workflows/update-prices.yml` が1日2回動く。

| 時刻(JST) | 内容 |
|---|---|
| **6:20** | 記事1本生成 → Instagram投稿画像作成 → 価格収集 → 新作検出 → サイト公開 → **Instagram投稿** |
| **18:20** | 価格収集 → サイト公開（記事生成・Instagram投稿はしない） |

所要は2〜3時間。**毎時0分はGitHub側が混雑して最大50分遅れる**ため20分にずらしてある。

### 設計上、絶対に壊してはいけない約束

- `concurrency.cancel-in-progress: false` … 進行中の実行を止めない。
  以前 true にしていて、価格収集の最中にpushして収集ごと打ち切る事故を3回起こした。
- **価格データが無いビルドは公開しない**（`Check price data before deploying`）。
  push実行は収集を飛ばすので、そのまま公開すると中身の無いサイトが良い版を上書きする。
- 夕方の回を判定する `github.event.schedule != '20 9 * * *'` が**8箇所**ある。
  cronを変えるときは全部揃えること。揃え忘れると夕方にも記事生成とInstagram投稿が走る。

---

## 3. 収益の構造（配置を考えるときの前提）

| 経路 | 1件あたり | 状態 |
|---|---|---|
| **買取リード（みんなの買取）** | **7,500〜10,000円** | 稼働中 |
| eBay (EPN) | 4%・上限$550 | 稼働中 |
| 楽天 | 2%・**1商品1,000円上限** | 稼働中 |
| Yahoo! (バリューコマース) | 約1% | 稼働中 |
| 米田屋（もしも） | 要確認 | 稼働中 |
| AdSense | 微額 | 稼働中 |

**買取が楽天の約10倍**。しかも購入という重い決断が要らずフォーム入力だけで成立する。
だから買取導線を最優先で配置している（ヘッダー「時計を売る」／`/sell/`／出品の無いページでは最上部）。

eBayは単価が大きいが**成果の有効期間が24時間**しかなく、最終クリック方式。
契約は**0日前通知で変更・解除できる**ので、収益の柱にしてはいけない。

---

## 4. 規約の制約（これを破るとサイトが死ぬ）

### 楽天ウェブサービス規約 第8条4項 ← 最重要

> ウェブサービスを使用した部分において、楽天のサイト以外へのリンクを設けてはならない

**楽天から取得した写真・価格を表示している領域からは、楽天以外へリンクできない。**
違反すればAPI利用停止＝価格も写真も取れなくなり、サイトの中身が全部消える。

対応済みの箇所（変更するときは必ずこの制約を思い出すこと）:

| 場所 | 写真・価格のリンク先 | 名前・仕様のリンク先 |
|---|---|---|
| モデルカード | 出品ページ | 自サイトのモデルページ |
| ブランドカード | 出品ページ | 自サイトのブランドページ |
| 今日の一本 | 出品ページ | （自サイトへのリンクを置かない） |
| 記事の商品写真 | 出品ページ | — |
| 記事冒頭の枠 | 出品ページ | — |
| ブログのカード | **楽天の写真を使わない**（発祥地の実写に差し替え） | 記事ページ |

### 第10条
取得データを登録済みアプリ以外の用途に使えない。
→ **Instagramに楽天の商品写真・価格を載せられない。**

### その他
- 楽天API: 取得データの恒久保存・再配布禁止 → `data/prices/` は gitignore
- クレジット表記義務: フッターに "Supported by Rakuten Developers" / "Webサービス by Yahoo! JAPAN"
- 景表法・ステマ規制: PR表示（フッターに配置）
- eBay EPN: **自分で宣伝素材を作る場合・APIで仕組みを作る場合は事前の書面承認が必要**（申請済み・返信待ち）
- Wikimedia の CC BY / CC BY-SA 画像: 作者・ライセンス・出典の表示が必須（実装済み）

---

## 5. アカウントと鍵の在り処

**値はここに書かない。** 保管場所だけ記す。

| 用途 | 保管場所 |
|---|---|
| 楽天・Yahoo!・Instagram の鍵 | GitHub の Settings → Secrets and variables → Actions |
| 手元の作業用 | `watch-price-navi/.env`（gitignore済み） |
| 控え | `Desktop/アフィリエイト用/アフィリエイトIDなど.txt`（リポジトリ外） |

| サービス | 識別子 | 状態 |
|---|---|---|
| eBay Partner Network | Account 7621736 / Campaign 5339190423 | 稼働中 |
| Instagram | @watch_price_navi / IG_USER_ID 17841441194043461 | 稼働中。**トークンは毎日自動で更新され Secrets に書き戻される**ので手作業は不要 |
| Meta アプリ | watch-price-navi / App ID 1372830094291068 | **未公開のまま**。Instagramログイン方式の投稿権限は「テスト準備完了」なので公開もビジネス認証も不要 |
| GitHub PAT | Secrets の GH_PAT | Secretsの書き換え専用。トークン自動更新に使う |
| Google Search Console | 所有権確認済み・サイトマップ送信済み | 稼働中 |

---

## 6. いま待っているもの

| 件 | 状態 | 次にすること |
|---|---|---|
| **eBay Developers Program** | 審査中（2026-08-15申請、最低1営業日 → **8/17以降**） | ログインできたら Application Keysets の Production の App ID / Cert ID を取得 → Browse API で海外価格を取り込む |
| **EPNへの書面承認** | メール送信済み・返信待ち | 承認されれば eBay の商品画像を Instagram に使える |
| Metaアプリの公開 | 未公開のまま（ビジネス認証が必要） | 公開しなくても投稿はできている。必要になったら個人として認証する |
| afb / アクセストレード / タウンライフ | 審査中 | 成果URLが届いたら `data/kaitori.json` の該当サービスの `url` に入れるだけで表示される |


---

## 7. これからやること

### すぐ着手できるもの
- [ ] **eBay価格の取り込み**（承認待ち）。方針は「国内の最安値」と「海外の参考価格」を**別枠**にする。
      理由: eBayの表示価格をそのまま円換算すると、送料・関税・輸入消費税で1〜2割増えるため嘘になる。
- [ ] **Instagram投稿に時計の実写を載せる**（EPNの承認待ち）。
      現状は発祥地・創業者・スタイリングの写真のみ（58点）。
      Wikimediaから時計の実写を集める仕組み（`scripts/fetch-watch-photos.mjs`）は作ったが、
      **高級時計の写真がほとんど無く60モデル中4件しか取れなかった**ので実用にならない。
- [ ] 既存5記事に創業者・発祥地の写真が入っていない（本文がその語に触れていないため）。
      記事生成の指示は修正済みなので**今後の記事は自動的に入る**。既存分を直すならCI経由で再生成。

- [x] **記事の写真が日によって消える** → 価格収集で「記事が参照するモデル」を
      **ブランドをまたいで最優先**にした（2026-08-16）。
      記事の商品写真はそのモデルの価格データがあって初めて入る。取れなかった日は図版が消える。
      ブランドは目録順に処理されるので、モデルの優先順位を上げるだけでは足りなかった。
      修正前、IWCの記事参照18件に届くには先に12,575モデルを処理する必要があり
      （1件あたり約3秒なので10時間超）、100分の予算では原理的に到達できなかった。
      → 記事参照60件だけを回す「1周目」を先頭に足した。実測で60件ちょうど・4分。
      手元で実行して検証済み。記事の写真充足は 7/22が4/8→6/8、8/16が1/6→5/6 に改善。
      なお残る5件は国内に出品が無いモデル（A.ランゲ2点、IWCマークXI 等）で、順番ではなく在庫の問題。

### 品質面で残っている課題
- [ ] モデル名が型番だけの項目が約58%（16,000件）。出品タイトルから名前を拾えなかったもの。
- [ ] 4桁のみの型番が1,821件。中にキャリバーが紛れている可能性があるが、
      ロレックス1601のような本物のヴィンテージ型番と見分けがつかないため機械的に消せない。
      → `data/calibers.json` にブランドごとの一覧を足していくのが確実。

### 確認のしかた
- Instagram が投稿できているか確かめたいときは Actions の **Instagram check** を回す。
  1分で終わり、鍵・トークン・画像の到達性をその場で判定する。
  「実際に投稿する」にチェックを入れると本当に投稿する（日付を指定すればその日の分）。

### 依頼済みで未着手
- [ ] （なし。都度ここに追記する）

---

## 8. 過去に踏んだ地雷（同じ失敗を繰り返さないために）

1. **`cancel-in-progress: true`** … 価格収集を3回打ち切り、写真が2日間消えた。
2. **価格の無いビルドを公開** … 中身の無いサイトが良い版を上書きした。→ 関門を追加済み。
3. **ベルトが本体として最安値になる** … 下限を出品自体の中央値から計算していたため、
   ベルトばかりの型番では下限も下がって素通りしていた。→ `data/brand-min-price.json` を併用。
4. **ブルガリ ソロテンポを消しかけた** … `ST29S` をムーブメント記号と誤判定。
   シーガルの `ST1901` はST+4桁、ブルガリはST+2桁+英字。桁数で区別する。
5. **価格でモデルごと消そうとした** … 2,354件が対象になり、中に本物の型番が含まれていた。
   安いのは出品がベルトだからで、型番自体は実在する。**消すのはモデルではなく出品**。
6. **cronだけ変えて条件式を放置** … 夕方にも記事生成とInstagram投稿が走るところだった。
7. **正規表現の記号を外し忘れ** … `A. Lange` の `.` が任意の1文字として働き、
   19世紀の医学書のPDFを大量に拾った。
8. **Wikimediaの画像をファイル名だけで判定** … 店舗の建物・コンセプトカー・偽造品を拾った。
9. **優先順位をブランドの中だけで上げた** … 外側のブランドの並びが変わらないので
   後ろのブランドは丸ごと時間切れで飛ぶ。優先はブランドをまたいで効かせること。
   → Commonsの**カテゴリ**で「時計である」ことを確かめる。

---

## 9. よく使うコマンド

```bash
npm run validate                          # データの整合性チェック
npx tsc --noEmit                          # 型チェック
node scripts/prune-auto-catalog.mjs       # カタログのゴミを確認（--apply で除去）
node scripts/build-instagram-post.mjs     # 投稿画像を作る（--force で作り直し）
node scripts/post-to-instagram.mjs --dry-run   # 投稿の下見
node scripts/fetch-heritage-images.mjs    # 創業者・発祥地の写真を取得
```

デザインを変えたときは、**手元で `npx next dev` を起動して実際に描画し、
プレビューを Artifact として公開して目で確かめてから** 送信すること。
過去、確認せずに送って作り直しになったことが複数回ある。
